#### Providing a container

  In InstantSearch.js, widgets are rendered within a `container`—a DOM element to inject the widget into. Your [render function](#rendering-a-custom-user-interface) should accept a `container` CSS selector as a string (such as `#negative-refinement-list`) or a direct reference to the element, as an `HTMLElement`.